#include <stdio.h>
/**
 * main - entry block
 * @void: no argument
 * Return: Always 0 (Success)
 **/
int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
		printf("%d", k);

	printf("\n");
	return (0);
}
