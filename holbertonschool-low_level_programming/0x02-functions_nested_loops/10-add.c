#include "holberton.h"
/**
 * add - function that adds two integers together
 * @x: is an integer
 * @z: is an integer
 * Return: result of integers x and z
 */
int add(int x, int z)
{
	int c;

	c = x + z;
	return (c);
}
